%table.table.table-responsive
  %thead
    %th ID
    %th Priority
    %th Allow
    %th Actions
    %th Conditions
  %tbody
    %tr
      - @policy.targets.order([:priority, :desc]).each do |target|
        = render 'permissions/targets/target', target: target
