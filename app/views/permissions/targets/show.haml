%ol.breadcrumb
  %li= link_to 'Policies', policies_path
  %li= link_to @policy.name, policy_path(@policy)
  %li= link_to 'Targets', policy_targets_path(@policy)
  %li.active= @target.id
%h1
  Target
  %small
    ID:
    = @target.id
  .pull-right
    = render 'partials/modtools'
.well
  %pre= JSON.pretty_generate(@target.serializable_hash).gsub(/\n/, '<br/>').html_safe
