%ol.breadcrumb
  %li= link_to 'Users', users_path
  %li= link_to @user.name, user_path(@user)
  %li= link_to 'Authorizations', edit_user_path(@user)
  %li.active
    = @authorization.provider

= render 'form'