.red-white-display.closed
  = link_to "javascript:;", "data-content" => "#{render 'partials/nav/closure_info'}", "data-html" => :true, "data-placement" => :bottom , "data-toggle" => :popover, "data-container" => :nav do
    - if @active_closure.type == 'snow'
      = fa_icon 'snowflake-o'
    - else
      = fa_icon 'exclamation-triangle'
    School is closed
