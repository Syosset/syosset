- if peek_enabled?
  #peek{:class => "#{Peek.env}#{' disabled' if cookies[:peek] == 'false'}", "data-request-id" => Peek.request_id}
    .container
      - Peek.views.each do |view|
        .view{:id => view.dom_id}
          = render view.partial_path, view: view
